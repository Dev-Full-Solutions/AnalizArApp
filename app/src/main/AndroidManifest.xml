<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_analizar_launcher"
        android:label="AnalizArApp"
        android:roundIcon="@mipmap/ic_analizar_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Analizarapp"
        tools:targetApi="31">
        <activity
            android:name=".Mi_Cuenta"
            android:exported="false" />
        <activity
            android:name=".MisAlarmas1"
            android:exported="false" />
        <activity
            android:name=".ContactoActivity"
            android:exported="false" />
        <activity
            android:name=".Onboarding3"
            android:exported="false" />
        <activity
            android:name=".olvidaste_contrasena"
            android:parentActivityName=".login">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".login" />
        </activity>
        <activity
            android:name=".login"
            android:exported="false" />
        <activity
            android:name=".AlarmasConfiguracionActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>
